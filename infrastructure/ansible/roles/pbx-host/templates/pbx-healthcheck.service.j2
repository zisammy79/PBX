[Unit]
Description=PBX Platform health validation
After=docker.service

[Service]
Type=oneshot
User=root
WorkingDirectory={{ pbx_install_root }}
EnvironmentFile={{ pbx_env_file }}
ExecStart={{ pbx_install_root }}/scripts/validate-deployment-assets.sh --health-only
