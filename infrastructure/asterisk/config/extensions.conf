[general]
static = yes
writeprotect = no
clearglobalvars = no

[globals]
PBX_PLATFORM = pbx-platform

[default]
exten => _X.,1,NoOp(Unauthenticated context — rejecting)
 same => n,Hangup(403)

#include pbx-generated/active/extensions-tenants.conf
