[general]
dateformat = %F %T
queue_log = yes

[logfiles]
console => notice,warning,error
; Avoid notice-level SIP trace flooding messages (filled 12GB on prod). Warnings/errors only.
messages => warning,error
